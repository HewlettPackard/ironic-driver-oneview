============
Installation
============

At the command line::

    $ pip install networking-oneview

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-oneview
    $ pip install networking-oneview
